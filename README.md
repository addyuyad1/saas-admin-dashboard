# SaaS Admin Dashboard

This workspace contains the base folder structure for an Angular SaaS admin dashboard.

Empty folders are preserved with `.gitkeep` files so the structure can be committed immediately.
